🌐 Git and GitHub Workflow

  Do this s VSCode prompt
    1. Initialized a Git repo: git init

  Do this s VSCode prompt
    2. Tracked your files: git add .

  Do this s VSCode prompt
    3. Committed changes: git commit -m "Initial commit"

 Before doing the STEP4.  Create a new repo in your GITHUB account; copy the https ()
  Do this s VSCode prompt
    4. Created a GitHub repo and linked it: git remote add origin {https () goes here; the one above}...

  Do this s VSCode prompt
    5. Pushed to GitHub: git push -u origin main

Now your project lives both locally and online on GitHub.
